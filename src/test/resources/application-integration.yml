spring:
  datasource:
    url: jdbc:tc:postgresql:///postgres?TC_DAEMON=true&TC_INITSCRIPT=init_database.sql
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: postgres
    password: postgres
